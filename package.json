{
  "name": "classic-role-play-site-root",
  "version": "1.0.0",
  "description": "Classic Project - SAMP Server Website (Root)",
  "private": true,
  "scripts": {
    "start": "cd frontend && npm start",
    "build": "cd frontend && npm run build",
    "install": "cd frontend && npm install"
  }
}
